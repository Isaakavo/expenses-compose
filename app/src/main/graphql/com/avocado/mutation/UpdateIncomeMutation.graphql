mutation UpdateIncome($input: UpdateIncomeInput) {
    updateIncome(input: $input) {
        id
        userId
        total
        createdAt
        updatedAt
        comment
        paymentDate {
            date
            fortnight
        }
    }
}