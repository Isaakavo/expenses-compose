query ExpensesByFortnight($input: PayBeforeInput!) {
    expensesByFortnight(input: $input) {
        id
        userId
        total
        concept
        payBefore
        tags {
            id
            name
        }
        comment
    }
}