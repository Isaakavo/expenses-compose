query ExpensesByFortnight($payBefore: Date!) {
    expensesByFortnight(payBefore: $payBefore) {
        id
        userId
        total
        concept
        payBefore
        tags {
            id
            name
        }
        comment
    }
}