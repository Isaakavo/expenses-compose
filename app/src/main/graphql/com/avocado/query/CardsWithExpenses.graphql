query CardsWithListExpenses($cardId: ID!) {
    cardWithListExpenses(cardId: $cardId) {
        totalByMonth {
            ...totalFragment
        }
        totalByFortnight {
            ...totalFragment
            fortnight
        }
        expenses {
            ...expenseFragment
        }
    }
}