mutation CreateIncome($total: Float!, $paymentDate: Date!, $comment: String) {
    createIncome(total: $total, paymentDate: $paymentDate, comment: $comment) {
        userId
        total
        createdAt
        paymentDate {
            date
            fortnight
        }
    }
}