mutation CreateIncomeMutation($total: Float!, $paymentDate: Date!, $comment: String) {
    createIncome(total: $total, paymentDate: $paymentDate, comment: $comment) {
        id
        userId
        total
        createdAt
        paymentDate {
            date
            fortnight
        }
    }
}